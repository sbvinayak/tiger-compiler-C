 letExp(
  decList(
   typeDec(
    nametyList(
     namety(arrtype,
      arrayTy(int)),
     nametyList())),
   decList(
    varDec(arr1,
     arrtype,
     arrayExp(arrtype,
      intExp(10),
      intExp(0)),
     TRUE),
    decList(
     varDec(k,
      intExp(0),
      TRUE),
     decList(
      varDec(a,
       intExp(10),
       TRUE),
      decList(
       varDec(c,
        opExp(
         PLUS,
         opExp(
          DIVIDE,
          intExp(34),
          intExp(9)),
         intExp(6)),
        TRUE),
       decList(
        varDec(y,
         opExp(
          PLUS,
          varExp(
           simpleVar(a)),
          intExp(100)),
         TRUE),
        decList(
         varDec(z,
          opExp(
           PLUS,
           varExp(
            simpleVar(a)),
           intExp(12)),
          TRUE),
         decList(
          varDec(b,
           opExp(
            PLUS,
            opExp(
             MINUS,
             opExp(
              PLUS,
              opExp(
               PLUS,
               opExp(
                PLUS,
                intExp(7),
                opExp(
                 TIMES,
                 varExp(
                  simpleVar(a)),
                 varExp(
                  simpleVar(c)))),
               intExp(1)),
              opExp(
               TIMES,
               opExp(
                DIVIDE,
                opExp(
                 DIVIDE,
                 opExp(
                  TIMES,
                  intExp(12),
                  intExp(12)),
                 intExp(8)),
                intExp(2)),
               varExp(
                simpleVar(c)))),
             opExp(
              DIVIDE,
              intExp(44),
              intExp(11))),
            intExp(12)),
           TRUE),
          decList())))))))),
  seqExp(
   expList(
    varExp(
     simpleVar(arr1)),
    expList(
     assignExp(
      simpleVar(b),
      opExp(
       PLUS,
       intExp(9),
       varExp(
        simpleVar(a)))),
     expList(
      assignExp(
       simpleVar(c),
       opExp(
        PLUS,
        intExp(20),
        varExp(
         simpleVar(z)))),
      expList(
       assignExp(
        simpleVar(k),
        intExp(6)),
       expList(
        assignExp(
         simpleVar(b),
         opExp(
          PLUS,
          varExp(
           simpleVar(a)),
          varExp(
           simpleVar(y)))),
        expList())))))))